// src/main/scala/progscala2/typelessdomore/package-example1.scala
package com.example.mypkg

// 스칼라는 이름공간(namespace)에 package 를 사용하는 자바의 개념을 도입했지만, 파일 이름은 타입 이름과 일치할 필요가 없고, 패키지 구조는 디렉터리
// 구조와 일치할 필요가 없다. 패키지를 실제 물리적 위치와 무관하게 정의할 수 있다.

class MyClass {
  // ...
}
